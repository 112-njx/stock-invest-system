该文档中你将记录对前端系统修复的bug，格式按照：
时间：
修复bug内容（描述）：
的格式对该文档进行编写，要求编码内容简练而说明主要内容，
一次编写的编码内容描述在200字以内

---

时间：2026-08-10
修复bug内容（描述）：阶段四联调发现两个后端接口编排缺失，前端已按 roadmap 约定路径对接并做 404 占位（仅此两项处于占位态，其余 J/K/L/M/N 区、深度模式、步骤时间线均正常；后端补齐后前端自动生效，无需改动）：

1. **GET /api/v1/agent/runs（含 /api/v1/agent/runs/{id}）缺失**：roadmap 4.8.4「M 区 Agent 运行记录」要求按该接口展示多智能体运行历史与完整 agent_steps。后端当前仅有 /api/v1/agents（定制 Agent 配置 CRUD），无运行记录查询路由（agent_runs/agent_steps 表有数据但无接口）。前端 AgentRunsDialog 已按约定实现列表+详情（节点时间线），请求 404 时展示占位说明「运行记录接口后端尚未实现」。该问题为开发路线编排缺失：规划了接口但后端未实现。

2. **GET /api/v1/memory/files 缺失**：roadmap 4.6「记忆文件」按钮需展示/打开用户本地记忆文件夹，PageDesign 注明「由后端提供打开能力/返回本地路径」。后端当前仅配置 MEMORY_DIR，无返回记忆文件列表的接口。前端 MemoryFilesDialog 已按约定实现只读文件列表展示，请求 404 时展示占位说明（含记忆路径提示 data/memory/{user_id}/*.md）。同上为编排缺失：规划了能力但后端未实现。

以上两条已同步记录至 docs/Agent_backend/fixed.md，由后端在后续阶段补齐接口。

---

时间：2026-08-11
修复bug内容（描述）：行情页 G/H 区（IndexListPanel）与 D/E 区（WatchlistPanel）行此前只有单击联动 F 区 K 线，缺少双击打开第一层详情页（A/B/C/D 区）的事件，与需求「双击鼠标左键点开个股/ETF/行业指数具体K线」不符；另因固定指数无 K 线/快照数据，指数最新价、涨跌幅与行业关联 ETF 显示 "--"。已：① 两面板行增加 dblclick 事件，MarketView 接线跳转 /market/detail（单击仍联动）；② 依赖后端补齐固定指数数据（见 Agent_backend/fixed.md）。需重建前端（npm run build 或重启 dev）生效。

---

时间：2026-08-16
v0.1前端bug描述：
bug1：在首页 E/F/G/H/I区域（行情页）中，E和G区域（组件）的占比区域过小，导致部分文字无法显示或无法正常显示，部分列没有对齐。
例1：“大盘指数”的下方左边一栏“名称”不同于其他列，由于区域过小，导致这两个字竖状显示而非正常显示。
例2：“名称”下方的每一行由于区域过小，无法显示指数名称。
例3：其余列：最新价 涨跌幅 ETF 机会，下方显示数据偏左而非正对齐。


bug2:在首页 E/F/G/H/I区域（行情页）中，重点关注列如果用户没有添加相应股票，下方文字显示：暂无重点关注，可在下方搜索添加。
实际上搜索栏并不是在上方。将其文字改成：暂无重点关注，可在上方菜单搜索添加。相关添加逻辑之后会进行修改.


bug3:行情页·第一层（双击进入 A/B/C/D 区），B区域技术指标显示异常。

bug4:

v0.1前端优化描述：
优化1：在首页 E/F/G/H/I区域（行情页）中，右下角I区域的上方左边的头像和用户姓名前端设计，改为文字描述下方的设计（更改不算很大）：
布局：水平横向布局，整体为列表条目样式（类似移动端设置项 Cell）
左侧：圆形头像容器
容器：圆形border‑radius:50%，固定宽高，内部放置用户头像。
右侧区域，分为上下两行文本，垂直方向堆叠
第一行：显示该用户的用户名，字体大、字重正常；文本右侧紧跟一个灰色右箭头>（跳转指示器）；
第二行：文字“免费用户”，字体更小，颜色浅灰色；
对齐规则
头像与右侧文本块垂直居中对齐；
右侧：该用户的用户名和箭头在同一行，文字“免费版”在下方，左对齐，和用户名左端对齐。
间距：头像与右侧文字之间留有水平 margin；行与行之间有小垂直间距；整个 Cell 可点击跳转。
组件更改：
删除行情页菜单栏最右方的“退出”按钮（作用是退出登录）。以及其左边的用户头像和用户名。
添加逻辑：
交互触发：点击用户条目右侧>箭头，弹出浮层弹窗（下拉菜单 / 侧边弹窗），浮层悬浮于原有页面下方，底层页面保留。
整体布局
弹窗容器：和当前页面整体配色相同的圆角卡片，垂直方向列表布局，属于弹出式菜单 Popover，浮在主页面之上；
内部是多条ListCell列表项，从上到下垂直排布；
分割线：部分列表项之间有浅灰色水平分割线，对菜单做分组；
每一行 Cell 统一为水平 flex 布局：左侧图标 + 文本。
将原来的“退出”按钮（作用是退出登录）的逻辑变更到弹出式菜单中，图标描述：样式：线性图标（轮廓线稿），黑色线条，无填充。
图形构成：左侧是一个开口朝右的方框轮廓（C 形，矩形框去掉右侧竖边），一支箭头从方框内部水平向右穿出，箭头头部在方框外侧。
文本是退出登录。
用户头像的逻辑以后会更改，先使用默认头像。


优化2：在首页 E/F/G/H/I区域（行情页）中，行业指数列表显然列很宽松，但是列最新价 涨跌幅 ETF 机会的中间间距不唯一，
（除了名称之外）将列最新价 涨跌幅 ETF 机会的中间间距修改成唯一（整体靠右即可），同时根据列中需要显示数字的长度合理
分配间距使其每一列显示数字（或文字）中对齐。

优化3：在首页 E/F/G/H/I区域（行情页）中，I区域的“显示风格”用于切换主题的明亮和黑暗主题，将其右边切换显示风格的组件进行如下重构
整体：移动端列表 Cell 条目，水平横向布局。
左侧组件：圆形
容器：正圆形。
内部：如果该正圆形在左边，显示“🌙”图标，表示点击该按键后，切换成暗黑风格，反之则在右边，显示“☀”图标，表示
交互：点击左侧组件，正圆形组件变到右边，同时“🌙”图标变成“☀”图标，界面显示风格变成暗黑模式，反之亦然。
同时界面菜单栏右上角的页面风格图标也需要同时根据页面风格更改。
外层容器：浅灰色圆角矩形容器。

优化4：在首页 E/F/G/H/I区域（行情页）中，F区域和显示详细行情页的A区域K线图可以无限放大或者缩小，优化规定：
（1）缩小方案维持原状，不用更改。
（2）放大方案改为最大是在该区域显示15条k线，如果用户继续试图放大，弹出“不能再放大了喵!”

优化5：技术指标左侧的小齿轮作用更改。

优化6：

